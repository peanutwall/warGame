更新说明：
1.修改了战场，将它显示在了上方；
2.修改了一些函数的命名，如将comparePosition改为了detectPosition;
3.添加了每轮的士兵更新的计算和每个地点剩余士兵的计算，现在：
战场左上角标的数字即为A或B在该地剩余的士兵，该地属于何方由其颜色表示（A蓝B红）；
在AB各自基地下方标有其每轮开始可分配的士兵总数；
4.若A或B分配士兵时超出其士兵总数会在屏幕上显示错误；
5.修复了一些bug，如字迹重复显示等；
6.由于pygame播放动态数字比较困难，现在每轮结束后会将上一轮各方的策略显示在下方以供玩家参考；
7.添加功能：任意一方输入完策略后，点击shift（A左shiftB右shift）可以将其策略隐藏起来；
8.完成了核心计算函数的unittest；
